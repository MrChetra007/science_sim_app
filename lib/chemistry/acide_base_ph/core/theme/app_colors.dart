import 'package:flutter/material.dart';

class AppColors {
  // Background layers
  static const Color bgDeep     = Color(0xFF0D1117);  // deepest background
  static const Color bgSurface  = Color(0xFF161B22);  // card / panel background
  static const Color bgElevated = Color(0xFF21262D);  // elevated surface

  // Text
  static const Color textPrimary   = Color(0xFFE6EDF3);  // main text
  static const Color textSecondary = Color(0xFF8B949E);  // muted text
  static const Color textHint      = Color(0xFF484F58);  // placeholder / hint

  // Border
  static const Color borderDefault = Color(0xFF30363D);
  static const Color borderAccent  = Color(0xFF58A6FF);

  // Accent
  static const Color accentBlue    = Color(0xFF58A6FF);
  static const Color accentGreen   = Color(0xFF3FB950);
  static const Color accentPurple  = Color(0xFFBC8CFF);
  static const Color accentOrange  = Color(0xFFF0883E);
}
