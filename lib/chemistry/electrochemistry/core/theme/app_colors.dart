import 'package:flutter/material.dart';

class AppColors {
  // Background layers
  static const Color bgDeep     = Color(0xFF0A0E17);  // deepest background
  static const Color bgSurface  = Color(0xFF131A26);  // card / panel background
  static const Color bgElevated = Color(0xFF1C2535);  // elevated surface

  // Text
  static const Color textPrimary   = Color(0xFFE2EAF4);  // main text
  static const Color textSecondary = Color(0xFF8899B0);  // muted text
  static const Color textHint      = Color(0xFF445566);  // placeholder / hint

  // Border
  static const Color borderDefault = Color(0xFF263040);
  static const Color borderAccent  = Color(0xFF4FAAFF);

  // Accent
  static const Color accentElectric = Color(0xFF4FAAFF);  // electron blue
  static const Color accentGreen    = Color(0xFF2ECC71);  // cathode / reduction
  static const Color accentAmber    = Color(0xFFF0A030);  // anode / oxidation
  static const Color accentRed      = Color(0xFFE85D30);  // warning / danger
  static const Color accentPurple   = Color(0xFFAA80FF);  // Nernst / equations
}
