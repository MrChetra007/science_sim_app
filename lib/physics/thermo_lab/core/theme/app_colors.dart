import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color bgDeep     = Color(0xFF0F1923);  // deep dark navy
  static const Color bgSurface  = Color(0xFF1A2535);  // card background
  static const Color bgElevated = Color(0xFF243045);  // elevated panel

  // Text
  static const Color textPrimary   = Color(0xFFE8EDF3);
  static const Color textSecondary = Color(0xFF8D9DB0);
  static const Color textHint      = Color(0xFF4A5568);

  // Borders
  static const Color borderDefault = Color(0xFF2D3E55);
  static const Color borderAccent  = Color(0xFFFF6B35);

  // Module accent colors
  static const Color accentHeat    = Color(0xFFFF6B35);  // orange-red — heat transfer
  static const Color accentGas     = Color(0xFF4FC3F7);  // sky blue — gas laws
  static const Color accentCarnot  = Color(0xFFFFD54F);  // amber — engine/work
  static const Color accentPhase   = Color(0xFF81C784);  // green — phase change
  static const Color accentEntropy = Color(0xFFCE93D8);  // purple — entropy
  static const Color accentLaws    = Color(0xFF80DEEA);  // teal — laws
}
